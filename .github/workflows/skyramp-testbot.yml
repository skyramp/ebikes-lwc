name: Skyramp Testbot

on:
  pull_request:
    types: [opened, synchronize, reopened, closed]
  issue_comment:
    types: [created]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.event.issue.number || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' && github.actor != 'skyramp-testbot[bot]' }}

jobs:
  testbot:
    # Only run for pull_request events or issue_comment on PRs mentioning @skyramp-testbot
    if: >-
      github.event_name == 'pull_request' ||
      (github.event_name == 'issue_comment' && github.event.issue.pull_request && contains(github.event.comment.body, '@skyramp-testbot') && github.event.comment.user.login != 'skyramp-testbot[bot]')
    runs-on: 'ubuntu-latest'
    permissions:
      contents: write
      pull-requests: write
      checks: write

    steps:
      # Generate a GitHub App installation token to retrigger CI workflows on push
      # https://docs.github.com/en/apps/creating-github-apps/authenticating-with-a-github-app/making-authenticated-api-requests-with-a-github-app-in-a-github-actions-workflow
      - name: Generate GitHub App Token
        id: skyramp-testbot-app-token
        uses: actions/create-github-app-token@d72941d797fd3113feb6b93fd0dec494b13a2547 # v1
        with:
          app-id: ${{ secrets.SKYRAMP_TESTBOT_APP_ID }}
          private-key: ${{ secrets.SKYRAMP_TESTBOT_APP_PRIVATE_KEY }}

      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 0
          token: ${{ steps.skyramp-testbot-app-token.outputs.token }}

      - name: Setup Salesforce CLI and Authenticate
        run: |
          npm install -g @salesforce/cli
          echo "${SFDX_AUTH_URL}" > /tmp/sfdx_auth.txt
          sf org login sfdx-url --sfdx-url-file /tmp/sfdx_auth.txt --alias mydevorg --set-default
          rm -f /tmp/sfdx_auth.txt
          SKYRAMP_TEST_TOKEN=$(sf org display --target-org mydevorg --json | jq -r '.result.accessToken')
          echo "SKYRAMP_TEST_TOKEN=${SKYRAMP_TEST_TOKEN}" >> $GITHUB_ENV
        env:
          SFDX_AUTH_URL: ${{ secrets.SFDX_AUTH_URL }}

      - uses: skyramp/testbot@e424c89ad41f20d2ae56cf1d6f20c94332017e2b # v0.10.2
        env:
          SFDX_AUTH_URL: ${{ secrets.SFDX_AUTH_URL }}
          SF_USERNAME: ${{ secrets.SF_USERNAME }}
          SF_PASSWORD: ${{ secrets.SF_PASSWORD }}
          SKYRAMP_TEST_TOKEN: ${{ env.SKYRAMP_TEST_TOKEN }}
        with:
          skyrampLicenseFile: ${{ secrets.SKYRAMP_LICENSE_FILE }}
          anthropicApiKey: ${{ secrets.SKYRAMP_TESTBOT_API_KEY }}
          githubToken: ${{ steps.skyramp-testbot-app-token.outputs.token }}
          targetSetupCommand: |
            set -euo pipefail
            sf project deploy start --source-dir force-app --target-org mydevorg --wait 10
            sf community publish -n E-Bikes --target-org mydevorg
            sf project deploy start --metadata-dir=guest-profile-metadata --target-org mydevorg --wait 10
            ORG_URL=$(sf org display --target-org mydevorg --json | jq -r '.result.instanceUrl')
            echo "{\"baseUrl\": \"${ORG_URL}\"}"
          targetReadyCheckCommand: |
            sf org display --target-org mydevorg --json | jq -e '.result.connectedStatus == "Connected"' > /dev/null
          targetReadyCheckTimeout: '120'
          authTokenCommand: |
            sf org display --target-org mydevorg --json | jq -r '.result.accessToken'
          skipTargetTeardown: 'true'
